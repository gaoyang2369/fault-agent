{
  "$defs": {
    "RequestSource": {
      "description": "请求进入应用服务的可信来源类型。",
      "enum": [
        "HTTP",
        "AGENT_TOOL",
        "INTERNAL"
      ],
      "title": "RequestSource",
      "type": "string"
    },
    "Role": {
      "description": "系统支持的用户角色。",
      "enum": [
        "GUEST",
        "ENGINEER",
        "ADMIN"
      ],
      "title": "Role",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "携带由认证边界确定的请求追踪信息、用户和角色。",
  "properties": {
    "request_id": {
      "minLength": 1,
      "title": "Request Id",
      "type": "string"
    },
    "trace_id": {
      "minLength": 1,
      "title": "Trace Id",
      "type": "string"
    },
    "user_id": {
      "maxLength": 128,
      "minLength": 1,
      "title": "User Id",
      "type": "string"
    },
    "roles": {
      "items": {
        "$ref": "#/$defs/Role"
      },
      "minItems": 1,
      "title": "Roles",
      "type": "array",
      "uniqueItems": true
    },
    "request_source": {
      "$ref": "#/$defs/RequestSource"
    }
  },
  "required": [
    "request_id",
    "trace_id",
    "user_id",
    "roles",
    "request_source"
  ],
  "title": "RequestContext",
  "type": "object",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:fault-agent:contract:1.0.0:request-context",
  "x-contract-version": "1.0.0"
}
